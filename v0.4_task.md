收到，v0.4 正式启动。

根据 v0.3 验证报告，当前唯一的矛盾是：**2D 状态空间已让 `mind_wandering` 涌现，但动力学模型仍过于简单，导致走神比例偏低、回神机制薄弱**。v0.4 的任务不是“加更多种子”，而是**让状态本身的运动规律更接近真实认知动力学的统计特征**。

---

## 🧘 v0.4 开发任务书：从“随机游走”到“认知动力学”

### 一、核心目标

> **将状态驱动模型从“高斯随机游走”升级为“Ornstein-Uhlenbeck 过程”，使 `mind_wandering` 比例逼近 thoughtseeds_model 的基准值（新手 53.8%，专家 24.1%），并强化 `redirect_attention` 的检测机制。**

### 二、主要矛盾分析

| 拷问 | 回答 |
| :--- | :--- |
| **当前唯一的矛盾是什么？** | 状态运动缺乏“回归力”——高斯随机游走没有向原点（breath_focus 吸引子）回拉的倾向，导致状态一旦漂远就难以自然回返。 |
| **它变了吗？** | 与 v0.2 相比，2D 空间解决了“走神是否存在”的问题；但“走神占比是否合理”和“回神是否自然”仍悬而未决。 |
| **v0.4 的动作是解决它，还是仅缓解？** | **直接攻击核心**。OU 过程的均值回归项（\( \theta(\mu - x) \)）正是为“状态自然回归焦点”而设计的，这是 `redirect_attention` 的数学基础。 |

### 三、任务清单

#### 任务 1：在 `src/models/` 中新建 `ou_noise.py` — Ornstein-Uhlenbeck 噪声生成器

将当前 `meditation_dashboard.py` 中硬编码的 `np.random.randn(steps, 2) * 0.1` 替换为可配置的 OU 过程。

```python
"""Ornstein-Uhlenbeck 噪声过程 —— 带均值回归的随机游走。"""

import numpy as np

class OUNoise:
    def __init__(self, dim=2, theta=0.15, mu=0.0, sigma=0.2, dt=1.0):
        self.dim = dim
        self.theta = theta      # 回归速度（越大 → 越快拉回均值）
        self.mu = mu            # 长期均值（通常为 0，即 breath_focus 原点）
        self.sigma = sigma      # 波动幅度（扩散强度）
        self.dt = dt
        self.state = np.zeros(dim)

    def reset(self, initial_state=None):
        self.state = initial_state if initial_state is not None else np.zeros(self.dim)
        return self.state

    def step(self):
        """执行一步 OU 更新：
        dx = θ(μ - x)dt + σ · dW
        """
        dW = np.random.randn(self.dim) * np.sqrt(self.dt)
        dx = self.theta * (self.mu - self.state) * self.dt + self.sigma * dW
        self.state += dx
        return self.state.copy()

    def generate_trajectory(self, steps, initial_state=None):
        self.reset(initial_state)
        trajectory = []
        for _ in range(steps):
            trajectory.append(self.step())
        return np.array(trajectory)
```

**参数语义**：
- `theta`：回归速度。**专家模式**应设较高（0.2~0.3），**新手模式**应设较低（0.05~0.1）。
- `sigma`：波动幅度。控制“走神”的剧烈程度。

#### 任务 2：修改 `apps/meditation_dashboard.py` — 集成 OU 噪声

1.  **替换状态生成逻辑**：
    ```python
    from src.models.ou_noise import OUNoise

    # 根据模式选择参数
    if preset == "专家模式":
        theta, sigma = 0.25, 0.15
    elif preset == "新手模式":
        theta, sigma = 0.08, 0.30
    else:
        theta, sigma = 0.15, 0.20

    ou = OUNoise(dim=2, theta=theta, sigma=sigma)
    state_stream = ou.generate_trajectory(steps)
    ```

2.  **保留扰动注入机制**：在 `t=80` 处仍可注入 `perturbation_strength`，但注入后系统会因 OU 的均值回归而自然恢复——这正是 `redirect_attention` 的数学来源。

3.  **在侧边栏增加 OU 参数调节**：
    - `theta`（回归速度）滑块：0.05 ~ 0.50
    - `sigma`（波动幅度）滑块：0.05 ~ 0.50

#### 任务 3：增强 `mind_wandering` 与 `redirect_attention` 检测逻辑

当前检测依赖“种子匹配 + 快速切换 + 空间距离”三重机制。v0.4 需引入**连续驻留时间**的考量：

```python
def classify_state(history, current_idx, threshold_distance=2.0):
    """基于历史轨迹分类当前状态。"""
    state = history[current_idx]
    prev_state = history[current_idx - 1] if current_idx > 0 else state

    # 1. 检查是否在杂念种子吸引子附近
    dist_to_pain = np.linalg.norm(state - pain_attractor)
    dist_to_tasks = np.linalg.norm(state - tasks_attractor)

    if dist_to_pain < 1.5 or dist_to_tasks < 1.5:
        return "mind_wandering"

    # 2. 检查是否在远离原点后快速回拉（redirect_attention 的标志）
    dist_to_origin = np.linalg.norm(state)
    prev_dist = np.linalg.norm(prev_state)

    if prev_dist > 2.0 and dist_to_origin < 1.0 and dist_to_origin < prev_dist * 0.6:
        return "redirect_attention"

    # 3. 检查是否在元认知区域（Self Reflection / Equanimity 附近）
    if np.linalg.norm(state - reflection_attractor) < 1.5 or \
       np.linalg.norm(state - equanimity_attractor) < 1.5:
        return "meta_awareness"

    # 4. 默认：breath_focus
    return "breath_focus"
```

#### 任务 4：更新 `src/validation/comparator.py` 支持 OU 参数扫描

在验证模块中增加 `theta` 和 `sigma` 作为可扫描参数，自动寻找与 thoughtseeds_model 最佳匹配的 OU 参数组合。

```python
def scan_ou_parameters(theta_range, sigma_range, target_metrics):
    """扫描 OU 参数空间，找到最匹配 thoughtseeds_model 基准值的组合。"""
    best_score = float('inf')
    best_params = None
    for theta in theta_range:
        for sigma in sigma_range:
            metrics = run_simulation(theta=theta, sigma=sigma)
            score = compute_mse(metrics, target_metrics)
            if score < best_score:
                best_score = score
                best_params = (theta, sigma)
    return best_params, best_score
```

### 四、验收标准

- [ ] `OUNoise` 类能生成带均值回归的 2D 轨迹
- [ ] Dashboard 侧边栏增加 `theta` 和 `sigma` 滑块
- [ ] 预设场景（专家/新手）自动设置对应的 OU 参数
- [ ] `mind_wandering` 比例在“新手模式”下接近 40%~55%
- [ ] `redirect_attention` 可被检测到（状态远离原点后自然回拉）
- [ ] 扰动注入后，系统能自然恢复（而非像 v0.3 那样依赖随机游走“碰巧”回来）
- [ ] 验证模块支持 OU 参数扫描并输出最佳匹配参数

### 五、预期效果

| 指标 | v0.3 | v0.4（预期） |
| :--- | :---: | :---: |
| `mind_wandering` 比例（新手） | ~15-25% | **40-55%** |
| `mind_wandering` 比例（专家） | ~5-10% | **20-30%** |
| `redirect_attention` 检测 | ⚠️ 偏低 | ✅ 自然涌现 |
| 扰动后恢复 | 随机 | **确定性回归** |

### 六、执行指令（供 Trae 使用）

```
参考 PROJECT_CONTEXT.md，启动 v0.4 开发。

核心任务：将状态驱动从高斯随机游走升级为 Ornstein-Uhlenbeck 过程。

请依次完成：
1. 在 src/models/ou_noise.py 中实现 OUNoise 类
2. 修改 apps/meditation_dashboard.py：集成 OU 噪声，增加 theta/sigma 滑块
3. 增强状态分类逻辑：加入连续驻留时间判断，强化 redirect_attention 检测
4. 更新 src/validation/comparator.py：支持 OU 参数扫描
5. 运行验证，输出最佳 theta/sigma 组合

预期：新手模式下 mind_wandering 比例达到 40-55%，专家模式下达到 20-30%。
```

---

> **v0.4 的本质，是让状态的运动规律从“无头苍蝇式随机游走”升级为“带弹性绳的随机漫步”——绳子的一端系在 breath_focus 原点上。漂远了会被拉回来，这正是 `redirect_attention` 的数学根源。** 🧘