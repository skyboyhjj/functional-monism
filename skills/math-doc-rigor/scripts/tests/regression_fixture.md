# 反例样本（用于回归测试 check_md.py）

## emoji

| 项 | 状态 |
|:---|:---|
| 曲线的 RH | ✅ |
| 正则性 | ❌ |
| 朴素核验 | ✅ |

## 粗体断裂

Schatten 范数 —— 是**「定号型」记账** ，这里应该被判为断裂。

正常粗体 **这里有空格包裹** 是好的。

行首粗体 **是「定号型」记账** 也是好的。

中文**夹在汉字中间**同样成立。

算术 2 ** 3 前后都是空格，两侧都不能开合，不该报。

行内代码 `2 ** 3 不该报` 已被掩码。

```
围栏代码块里 **不该报**
```

    缩进代码块里 **不该报**

## 收尾

以上。
